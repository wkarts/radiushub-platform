@extends('layouts.app')
@section('title','Auditoria')
@section('content')
<x-page-header title="Logs e auditoria" description="Rastreabilidade de operações, resultados, origem e alterações, com dados sensíveis removidos." />
<div class="toolbar"><form class="filter-form"><input class="search-input" name="action" value="{{ request('action') }}" placeholder="Ação"><select name="result"><option value="">Todos os resultados</option><option value="success">Sucesso</option><option value="failed">Falha</option></select><button class="btn btn-secondary">Filtrar</button></form><span class="cell-subtitle">{{ $logs->total() }} evento(s)</span></div>
<div class="card"><div class="table-wrap"><table><thead><tr><th>Data</th><th>Usuário</th><th>Ação</th><th>Recurso</th><th>IP</th><th>Resultado</th><th></th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ optional($log->created_at)->format('d/m/Y H:i:s') }}</td><td>{{ $log->user_id ?: 'Sistema' }}</td><td><span class="kbd">{{ $log->action }}</span></td><td>{{ class_basename($log->auditable_type ?: $log->subject_type ?: '—') }}<div class="cell-subtitle">{{ $log->auditable_id ?: $log->subject_id }}</div></td><td>{{ $log->ip_address ?: '—' }}</td><td><x-status-badge :value="$log->result" /></td><td><button class="btn btn-secondary btn-sm" data-modal-open="audit-{{ $log->id }}">Detalhes</button></td></tr>@empty<tr><td colspan="7"><x-empty-state /></td></tr>@endforelse</tbody></table></div>@include('partials.pagination',['paginator'=>$logs])</div>
@foreach($logs as $log)<x-modal id="audit-{{ $log->id }}" title="Detalhes da auditoria" size="lg"><div class="modal-body"><div class="form-grid"><div class="col-6"><label>Ação</label><div class="kbd">{{ $log->action }}</div></div><div class="col-3"><label>Resultado</label><x-status-badge :value="$log->result" /></div><div class="col-3"><label>Request ID</label><div class="kbd">{{ $log->request_id ?: '—' }}</div></div></div><div class="audit-diff" style="margin-top:16px"><div><label>Antes</label><pre class="code">{{ json_encode($log->old_values,JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre></div><div><label>Depois</label><pre class="code">{{ json_encode($log->new_values,JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre></div></div><label>Metadados</label><pre class="code">{{ json_encode($log->metadata,JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre></div><div class="modal-footer"><button type="button" class="btn btn-secondary" data-modal-close>Fechar</button></div></x-modal>@endforeach
@endsection
