@extends('layouts.app')
@section('title','Empresas')
@section('content')
<x-page-header title="Empresas da plataforma" description="Tenants com isolamento de clientes, rede, cobrança e credenciais."><x-slot:actions><button class="btn btn-primary" data-modal-open="tenant-create">＋ Nova empresa</button></x-slot:actions></x-page-header>
<div class="card"><div class="table-wrap"><table><thead><tr><th>Empresa</th><th>Slug</th><th>Documento</th><th>Contato</th><th>Status</th><th></th></tr></thead><tbody>@forelse($tenants as $tenant)<tr><td class="cell-title">{{ $tenant->name }}</td><td><span class="kbd">{{ $tenant->slug }}</span></td><td>{{ $tenant->document ?: '—' }}</td><td>{{ $tenant->email ?: $tenant->phone ?: '—' }}</td><td><x-status-badge :value="$tenant->active?'active':'inactive'" /></td><td><div class="table-actions"><button class="btn btn-secondary btn-sm" data-modal-open="tenant-edit-{{ $tenant->id }}">Editar</button><form method="post" action="{{ route('platform.tenants.destroy',$tenant) }}">@csrf @method('delete')<button class="btn btn-ghost btn-sm" data-confirm="Excluir empresa vazia?">Excluir</button></form></div></td></tr>@empty<tr><td colspan="6"><x-empty-state /></td></tr>@endforelse</tbody></table></div>@include('partials.pagination',['paginator'=>$tenants])</div>
<x-modal id="tenant-create" title="Nova empresa" size="lg"><form method="post" action="{{ route('platform.tenants.store') }}">@csrf @include('tenants._form',['tenant'=>null])</form></x-modal>
@foreach($tenants as $tenant)<x-modal id="tenant-edit-{{ $tenant->id }}" title="Editar empresa" size="lg"><form method="post" action="{{ route('platform.tenants.update',$tenant) }}">@csrf @method('put') @include('tenants._form',['tenant'=>$tenant])</form></x-modal>@endforeach
@endsection
