@extends('layouts.app')
@section('title','Contratos')
@section('content')
<x-page-header title="Contratos" description="Vínculo comercial entre cliente, credencial, plano e recorrência financeira."><x-slot:actions><button class="btn btn-primary" data-modal-open="contract-create">＋ Novo contrato</button></x-slot:actions></x-page-header>
<div class="card"><div class="table-wrap"><table><thead><tr><th>Contrato</th><th>Cliente</th><th>Acesso</th><th>Plano</th><th>Mensalidade</th><th>Próxima fatura</th><th>Status</th><th></th></tr></thead><tbody>@forelse($contracts as $contract)<tr><td class="cell-title">{{ $contract->number }}</td><td>{{ $contract->subscriber->name }}</td><td>{{ $contract->access->username }}</td><td>{{ $contract->plan->name }}</td><td>R$ {{ number_format((float)$contract->amount,2,',','.') }}</td><td>{{ optional($contract->next_invoice_at)->format('d/m/Y') ?: '—' }}</td><td><x-status-badge :value="$contract->status" /></td><td><div class="table-actions"><button class="btn btn-secondary btn-sm" data-modal-open="contract-edit-{{ $contract->id }}">Editar</button><form method="post" action="{{ route('contracts.destroy',$contract) }}">@csrf @method('delete')<button class="btn btn-ghost btn-sm" data-confirm="Excluir contrato cancelado?">Excluir</button></form></div></td></tr>@empty<tr><td colspan="8"><x-empty-state /></td></tr>@endforelse</tbody></table></div>@include('partials.pagination',['paginator'=>$contracts])</div>
<x-modal id="contract-create" title="Novo contrato" size="lg"><form method="post" action="{{ route('contracts.store') }}">@csrf @include('contracts._form',['contract'=>null])</form></x-modal>
@foreach($contracts as $contract)<x-modal id="contract-edit-{{ $contract->id }}" title="Editar contrato" size="lg"><form method="post" action="{{ route('contracts.update',$contract) }}">@csrf @method('put') @include('contracts._form',['contract'=>$contract])</form></x-modal>@endforeach
@endsection
