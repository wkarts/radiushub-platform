# Clientes NAS são carregados dinamicamente da tabela mikrotik_devices pelo módulo SQL.
# O cliente local existe apenas para testes administrativos no próprio servidor.
client localhost {
    ipaddr = 127.0.0.1
    secret = "@@RADIUS_LOCAL_SECRET@@"
    nas_type = other
}
