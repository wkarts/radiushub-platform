# FinancialTransactionService

Acesso via fachada: `$asaas->financialTransaction`.

| Método (SDK) | HTTP | Path |
|---|---|---|
| `retrieveExtract` | GET | `/v3/financialTransactions` |

## Exemplo rápido

```php
$result = $asaas->financialTransaction->retrieveExtract(
    pathParams: [],
    query: [],
    headers: [],
    payload: null
);
```
